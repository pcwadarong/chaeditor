import { uploadEditorFile } from '@/entities/editor/api/upload-editor-file';

/**
 * Connects the current app's attachment upload implementation to the editor UI.
 */
export const uploadEditorFileAdapter = uploadEditorFile;
