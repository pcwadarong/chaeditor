import { uploadEditorVideo } from '@/entities/editor/api/upload-editor-video';

/**
 * Connects the current app's video upload implementation to the editor UI.
 */
export const uploadEditorVideoAdapter = uploadEditorVideo;
