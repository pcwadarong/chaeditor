import { uploadEditorImage } from '@/entities/editor/api/upload-editor-image';

/**
 * Connects the current app's image upload implementation to the editor UI.
 */
export const uploadEditorImageAdapter = uploadEditorImage;
